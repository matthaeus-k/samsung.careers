<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TestBoard">
    <select id="selectAll" resultType="TestBoard">
        SELECT
          *
        FROM
          TEST_BOARD
        WHERE IS_DLT = 'N'
    </select>

    <select id="selectOne" resultType="TestBoard">
        SELECT
            *
        FROM
            TEST_BOARD
        where IDX = #{idX}
    </select>

    <insert id="insert">
        INSERT INTO TEST_BOARD(
        TITLE ,
        CONTENT ,
        NAME ,
        IS_DLT ,
        DT_REG,
        DT_UPD,
        VIEW_CNT
        )VALUES(
        #{title,jdbcType=VARCHAR} ,
        #{content,jdbcType=VARCHAR},
        #{name,jdbcType=VARCHAR},
        'N',
        #{dtReg},
        #{dtUpd},
        0
        );
    </insert>


    <update id="update">
        UPDATE TEST_BOARD
            SET
                TITLE = #{title,jdbcType=VARCHAR},
                CONTENT = #{content,jdbcType=VARCHAR},
                DT_UPD = #{dtUpd}
        WHERE IDX = #{idx}
    </update>

    <update id="delete">
        UPDATE TEST_BOARD
        SET
            IS_DLT = #{isDlt},
            DT_UPD = #{dtUpd}
        WHERE IDX = #{idx}
    </update>

    <update id="updateViewCnt">
        UPDATE TEST_BOARD
        SET
            VIEW_CNT = #{viewCnt}
        WHERE IDX = #{idx}
    </update>
</mapper>

